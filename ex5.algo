ALGORITHM Opérations sur un entier
VAR
    x,a: INTEGER
BEGIN
Write("Entrer la valeur de x");
        Read(x)
        a <- 0
    WHILE (y=1 ou y=2 ou y=3) DO
        REPEAT
        Write("Entrer une valeur entre 1 et 4");
        IF (x <= 1 and x >= 3) THEN
        IF (x = x+1;) THEN
            a <- a + 1
        ELSE_IF ( x = x*2) THEN
           a <- a*2
        ELSE
            a = a-4;
        END_IF
        read (a)
        END_IF
    UNTIL (x=4)
    END_WHILE
END